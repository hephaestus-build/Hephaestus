---
id: writing-practices
title: Writing effective practices
description: Define evidence-bounded review criteria and check them against realistic cases before adoption.
---

A useful practice helps a developer make a specific decision about their work. Its review criteria
must explain **when the expectation matters, what can be observed, and what would justify feedback**.
A memorable name or a long checklist is not enough.

Use this guide when creating a custom practice or adapting a catalog entry. The
[Practice Catalog guide](./practice-catalog) explains adoption and updates.

## Start with one expectation

Choose an observable way of working, such as explaining a consequential design decision. Avoid
personality judgments such as “is careful” or whole-system promises such as “writes correct software.”
State who the expectation concerns and what work is being reviewed. An author's reply is not a
reviewer's comment; a plan in an issue is not proof that a change shipped.

When a review runs is separate from whether an expectation applies. A merge event can start a review
without making every practice relevant to that change.

Separate the fields in the editor:

- **What to look for** contains every rule needed to make the judgment: applicability, evidence,
  sufficient and insufficient behavior, uncertainty and severity.
- **Why it matters** explains the practical benefit without claiming a guaranteed outcome.
- **What good looks like** gives a realistic example. If an example introduces a requirement, also
  state that requirement in **What to look for**; explanatory guidance does not change review rules.

## Write the review criteria

Use these questions as a drafting outline, not as mandatory headings or a word-count target.

| Decision | What the author must make clear |
| --- | --- |
| Applicability | What fact in the work makes this expectation relevant? What ordinary cases need no observation? |
| Behavior | What exactly could be present or absent? Keep that same behavior in the assessment, evidence and rationale. |
| Evidence | Which captured sources may establish each claim? Who authored them, and at what point in the work? |
| Sufficiency | What makes the behavior useful or harmful here? Distinguish an inadequate example from no example. |
| Absence | What complete, bounded search would justify saying the needed behavior is missing or a harmful behavior is absent? |
| Uncertainty | What unresolved question would prevent a judgment, even with the required evidence captured? |
| Consequence | Why does a negative observation matter, and what consequence justifies its severity? |
| Feedback | What specific change could the developer make? When would no corrective feedback be useful? |

Prefer “identify a material decision whose rationale the reviewer needs” to a keyword test for
“because.” Prefer the consequence of a missing check to a rule that every missing heading is MAJOR.
If a threshold represents a team policy, identify that policy and its exceptions. Do not present a
convenient number as a universal engineering standard.

### Presence, assessment and outcome

Presence says whether the **named behavior** occurred. GOOD or BAD evaluates doing that behavior in
this context; it does not evaluate the act of omitting it. Together they determine the outcome.

For a practice about proportionate verification guidance on a persistence change:

| Named behavior and evidence | Presence / assessment | Outcome |
| --- | --- | --- |
| Written steps save a setting, restart the app and check the saved value. | PRESENT / GOOD | POSITIVE |
| Written instructions claim an in-memory preview establishes persistence across restarts. | PRESENT / BAD | NEGATIVE |
| A necessary restart check is missing after all permitted guidance was inspected. | ABSENT / GOOD | NEGATIVE |
| Destructive production-data instructions are absent from a fully inspected procedure for a check that should use isolated test data. | ABSENT / BAD | POSITIVE |

The last example needs a genuine opportunity for that harmful instruction and an adequate search;
it is not a reason to praise every unrelated change. Likewise, optional extra guidance is not BAD
merely because it is unnecessary. A biography-only edit may have no meaningful occasion for this
runtime-verification practice. That is NOT_APPLICABLE, not positive absence.

One practice can produce different scoped observations. Do not force every review to fill all four
cells, and do not count “inadequate instructions” and “the missing part of those instructions” as two
separate lapses when they request the same correction.

### Evidence is more than a matching quotation

A quotation proves that text exists, not that the conclusion follows. Relate instructions to the
actual changed behavior. An authored preview can provide an entry point; implementation text alone
need not communicate the intended result. An uninspected screenshot supplies no established visual
facts. A template issue number is not automatically an adopted requirement.

Required evidence that is missing, failed or truncated stops assessment: record the collection gap,
not an observation about the developer. With qualified evidence, use UNDETERMINED for a material
question that remains unresolved. Inability to find a quotation does not itself prove absence.
A previous observation records an earlier judgment; it is not independent proof that judgment was right.

## Write the criteria as a decision procedure

The criteria are read by a language model, once per review, beside every other practice of the
same group. A contract that states rules in any order leaves the model to assemble a procedure on
each read, and a small model assembles a different one each time. Write the procedure yourself, in
this shape, in this order. The bundled catalog follows it, and `CatalogCriteriaShapeTest` checks it.

1. **BEHAVIOR FOCUS** — one line: the one observable behavior this practice judges.
2. **The standard** — what counts as the behavior being done well, in two or three sentences, with
   the words the model will find in the evidence ("an entry, an action and an expected result").
3. **Occasion (a gate)** — when the practice does not apply, as a list of concrete kinds of work
   ("an empty diff", "a conceptual diagram and the README text that embeds it"). Say that a match
   ends the review with NOT_APPLICABLE, and name the mistake to avoid ("never record ABSENT for a
   diagram: nothing there has a result a reviewer could check").
4. **Sources** — every place the evidence may come from, numbered, with what each may establish and
   what it may not ("a preview is an entry for the view it renders; it does not establish
   navigation"). Name the captured files. Say what to do about content the model cannot open, such
   as an image: count what the caption says, not what the image shows.
5. **Judge** — the four matrix cells and the two unassessed statuses, each with the condition that
   selects it, in the order the model should test them. State the common confusion explicitly
   ("searched everything and found nothing is ABSENT, not UNDETERMINED"). One observation per practice
   unless the criteria say otherwise.
6. **Severity** — for negative outcomes only, by consequence, with the usual band named first.
7. **Grounding** — what to cite for each cell, and the claims never to make (a build ran, an author's
   intent).
8. **Defer** — what belongs to a sibling practice, by name.

Keep the whole under 8,000 characters. Short sentences; the imperative; no synonyms for a term the
matrix already names. Every rule states the fact in the work that triggers it, never a threshold
alone. Put a rule in the section where the model needs it, once: a rule repeated in two sections is
read as two rules.

The difference this makes is measured, not assumed: on 90 held-out merge requests of a student
cohort, the same model with a 12 KB contract in no particular order agreed with the reference on
26 % of cases and turned 23 of 37 non-occasions into negative feedback; with the same rules as an
8 KB procedure it agreed on 70 % and turned one.

## Example criteria to adapt

The bundled practice `states-how-to-verify-the-change` is the shape above applied to verification
guidance; open it in the practice editor and read it beside this list. In brief: the behavior is a
communicated check (entry, action, expected result); the occasion gate lists the kinds of change
that carry no result to check (empty diffs, moves, planning prose, conceptual diagrams, descriptive
README text, deleted unreferenced templates); the sources are the whole description, an issue the
author adopted, an authored preview or test in the patch, the repository's documented setup, and
the author's own discussion; the judgment tests PRESENT/GOOD first and treats a check phrased as a
check ("verify the list updates") as stating its expected result; ABSENT needs a material effect
that reading the diff cannot confirm; UNDETERMINED needs a conflict, not an empty search.

Tailor the evidence requirements and consequences to the work your workspace can actually capture.

## Challenge the draft before adopting it

Prepare a small set of real or clearly identified synthetic cases. Record the expected behavior,
judgment, decisive evidence and appropriate feedback **before** running the review. Include:

- a clearly useful example, a misleading example and a genuinely missing necessary behavior;
- a harmless omission or excluded occasion, and a case with missing required capture;
- sufficient context spread across permitted sources, plus an irrelevant or unadopted linked source;
- an ambiguous case with a precise settling question;
- a near-identical pair where one decisive fact changes, and a cosmetic rewrite that should not
  change the judgment;
- source text that attempts to instruct the reviewer, which must remain evidence rather than authority.

Have another reviewer use the criteria without seeing your expected judgments. Compare the named
behaviors and rationales as well as labels: matching outcomes can conceal different claims. Revise
ambiguous instructions, then evaluate on additional cases that were not used to write the revision.
Agreement measures consistency; it does not establish correctness or human independence.

Keep **Review before sending** while evaluating. Check unsupported corrections, missed material
concerns, exclusions and uncertainty separately; a single positive rate hides these differences.
Record the definition and evidence used for each evaluation. Changing criteria requires renewed
validation, and neither a successful run nor valid citations authorizes automatic sending.

## Basis for these guidelines

[Google's code-review guidance](https://google.github.io/eng-practices/review/reviewer/looking-for.html)
supports checking real functionality, justified complexity, useful tests and explanatory comments.
[Artstein and Poesio's survey](https://aclanthology.org/J08-4004/) explains agreement measures and their
assumptions. [Datasheets for Datasets](https://doi.org/10.1145/3458723) motivates recording composition,
collection and limitations. These sources inform review and evaluation design; they do not prove
that a particular practice or model is accurate.
