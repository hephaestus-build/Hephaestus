---
id: database-migration
sidebar_position: 8
title: Database Migration Workflow
description: How a schema change becomes a Liquibase changelog, and what CI checks.
---

Liquibase manages schema changes in the application server. This page is the procedure; the entity
conventions the drift gate reads are in `server/AGENTS.md` § Schema changes.

## Quick start

1. Modify the JPA entities.
2. Write the drift into this branch's changelog:

   ```bash
   vp run db:draft-changelog
   ```

   The command applies the whole chain to a disposable database, diffs it against the JPA model,
   and writes the difference as change sets into
   `server/application/src/main/resources/db/changelog/<timestamp>_changelog.xml`, with ids
   `<timestamp>-1`, `-2`, … and the author `hephaestus`. A branch gets one such file: the first run
   creates it and appends its `<include>` to `master.xml`; later runs add change sets to it. It then
   refreshes the ERD. With no drift it writes nothing.

3. **Review the change sets.** Keep the real deltas and remove what the diff only inferred. Prefer
   `renameColumn` over a drop-and-add pair, start new sequences at `1` unless there is a data
   migration plan, and follow the destructive-change policy below. Use preconditions to reject
   incompatible database states; use `onFail="MARK_RAN"` only when failure proves the intended change
   is already present. Provide a tested rollback when reversal is safe; otherwise document recovery
   through a verified backup in the operator instructions. If you edit the changelog, refresh the ERD:

   ```bash
   vp run db:generate-erd-docs
   ```

4. Add a release changeset: `vp exec changeset`, or hand-write `.changeset/<slug>.md`
   (`.changeset/README.md`). A Liquibase `<changeSet>` and a release changeset are different things
   and a schema change needs both; `verify-changesets` fails a PR that touches `db/changelog/`
   without one. Keep the summary user-facing; the release notes flag the migration automatically.
5. Commit the changelog, the `master.xml` edit, the changeset, and `docs/contributor/erd/schema.mmd`.

## Changelog immutability policy

The Liquibase changelog chain **is** the upgrade path: every production and self-hosted
instance upgrades by replaying exactly these files, in `master.xml` order. That works only
if history never changes underneath an instance that already ran part of it.

1. **Released changesets are never edited.** A changeset is *released* once it reaches
   `main` — from that moment it may already be applied to a production database. Editing it
   causes checksum errors at boot on upgraded instances while fresh installs silently get a
   different schema. Fix mistakes **forward** with a new changelog file. Liquibase
   [excludes preconditions, contexts and labels from checksums](https://www.liquibase.com/blog/what-affects-changeset-checksums)
   and ignores deleted changesets, so those edits desynchronise installed instances without any
   error.
2. **`master.xml` is append-only.** Never remove or reorder existing `<include>` entries;
   new changelogs go at the end.
3. **Destructive changes follow deprecate-then-remove across two releases.** Renaming or
   dropping a table/column that shipped in a release happens in two steps: release *N*
   stops writing/reading it (and migrates data if needed), release *N+1* drops it. A single
   release must stay bootable against the schema left behind by the previous one.

CI enforces 1 and 2 mechanically (see below); 3 is a review responsibility. The sole archival
transition is the verified v0.77.4 baseline: archived files remain byte-identical and test-only.
The baseline and subsequent migrations follow the same immutability policy. Existing installations
must follow the [baseline synchronization runbook](../admin/liquibase-baseline-runbook).

### Pending contractions

Objects the application stopped writing, to be dropped in the release after the one that shipped
the listed changelog. Each is excluded from the
drift gate in `--exclude-objects` (`server/application/build.gradle.kts`, `liquibaseDiff`); the
changeset that drops the object also removes its exclusion.

| Object | Changelog that stopped writing it | Exclusion to remove |
|---|---|---|
| `consent_notice` table | `1789067582535_changelog.xml` | `table:consent_notice` |
| `consent_decision.notice_sha256` column | `1789067582535_changelog.xml` | `column:notice_sha256` |
| `product_survey_submission` table | `1789117764980_changelog.xml` | `table:product_survey_submission` |

## CI validation

1. **Migration-chain replay, schema drift and ERD** — Build workflow, job *App Server: Generated
   artifacts*. `vp run db:check-drift` applies the full `master.xml` chain to an empty database
   from the repository's PostgreSQL image (`docker/postgres`) and diffs the result against the JPA
   entities; a changelog that does not parse or apply fails here. `vp run db:generate-erd-docs`
   then has to leave `docs/contributor/erd/schema.mmd` unchanged.
2. **Changelog immutability** — Security workflow, job *Dependencies, secrets, and policy*, step
   *Changelog immutability guard*. A git diff from the PR base commit fails if any file under
   `db/changelog/` was modified, renamed or deleted, or if an existing `<include>` in
   `master.xml` was removed, reordered or had its attributes changed. The v0.77.4 transition additionally requires the
   pinned original master and byte-identical archives; it is not a general squash bypass.
3. **Seeded release upgrade** — `release-upgrade.yml` boots the previous stable release, seeds
   account and workspace data, synchronizes the baseline when crossing its verified cut-point,
   then boots the candidate against the same database and checks
   readiness, the seeded relationships, the practice catalog and authenticated reads. Release
   publication depends on it; it also runs weekly.

The empty-database replay sets no context filter. It evaluates the full chain, but preconditions can
skip changesets; a successful replay does not prove that every change ran. It also does not reproduce
the context selection of a `dev` or `prod` boot (`spring.liquibase.contexts`).

Add focused migration tests for populated tables, rejected database states and any supported rollback.
Empty-database replay cannot prove that existing rows satisfy a new `NOT NULL` or `CHECK` constraint,
and it does not exercise rollback. Preconditions run during replay, but only against the states that
replay reaches. The JPA diff cannot establish equivalence for objects Hibernate does not model, such
as triggers, partitioning and partial-index predicates; verify those through database-level tests.

## Resources

- [Liquibase documentation](https://docs.liquibase.com/) for change set syntax.

## Observation outcome transition before 1.0

The observation-axis migration introduces `assessment_status` and permits null presence for unassessed
observations. Assessment now describes the fixed target, while outcome is derived from the
[presence × assessment matrix](./practice-feedback-language.md#observation-assessment-axes).
The migration swaps GOOD/BAD on historical ABSENT rows to preserve their previous positive/negative
outcome, retains severity for historical negative outcomes and clears it otherwise. It does not relabel
old evidence against the new catalogue. A historical negative row without severity blocks migration
rather than receiving an invented impact band.

Back up the database, resolve any reported inconsistent historical rows explicitly, and upgrade the
server, sandbox runtime, webapp and custom API consumers together. Consumers must read the derived
`outcome` for verdicts and metrics, not interpret `assessment = BAD` as a negative outcome. Preserve
schema versions on exported evaluation data; frozen legacy reports must not be silently reinterpreted.
