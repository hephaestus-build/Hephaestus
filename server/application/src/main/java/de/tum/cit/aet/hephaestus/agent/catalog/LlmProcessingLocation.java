package de.tum.cit.aet.hephaestus.agent.catalog;

/** Operator-declared processing location, independent of the model's funding or API protocol. */
public enum LlmProcessingLocation {
    UNCLASSIFIED,
    ON_PREMISES,
    PRIVATE_CLOUD
}
