---
"hephaestus": patch
---

Practice definitions retain validation of nested evidence and review settings without deprecated validation warnings. Server startup initializes sync subscriptions only after their service is fully constructed, and build checks report actionable diagnostics instead of avoidable compiler warnings.
