---
"hephaestus": patch
---

A practice review of a repository that keeps images or other binary files alongside its code now sees
the code. The repository snapshot a review reads has a size bound, and binary files were filling it:
on one large repository the documentation screenshots used the whole bound before the snapshot reached
a single source file, leaving every review to work from the change alone.
