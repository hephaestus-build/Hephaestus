---
"hephaestus": patch
---

Workspace setup and AI model settings use the refreshed interface controls while preserving saved AI choices, independent account linking, and read-only access to earlier conversations when AI is off. The model settings show one fact per model, who operates it, and one row per tier and purpose.
